<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/public/css/style.css"/>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet">

        <title>Tela de Cadastro</title>
    </head>
    <body>
        <div class="container">
            <div class="content">      
                <div id="login">
                    <form method="POST" action="/cadastroDB"> 
                        <h2>Cadastre-se</h2> 
                        <p> 
                            <label for="nome_login">E-mail</label>
                            <input id="nome_login" name="login" required="required" type="email" placeholder="Digite seu e-mail"/>
                        </p>
                        <p> 
                            <label for="nome_login">Nome de usuário</label>
                            <input id="nome_nome" name="nome" required="required" type="text" placeholder="Digite seu login"/>
                        </p>
                        <p> 
                            <label for="senha_login">Senha</label>
                            <input id="senha_login" name="senha" required="required" type="password" placeholder="Digite sua senha" /> 
                        </p>
                        <p> 
                            <label for="senhaConf_login">Confirme a Senha</label>
                            <input id="senhaConf_login" name="confSenha" required="required" type="password" placeholder="Confirme sua senha" /> 
                        </p>
                        <div class="entrar"> 
                            <input type="submit" value="Cadastrar"/> 
                        </div>
                    </form>
                    <% if(status == 401){ %>
                        <h3>As senhas não coincidem!</h3>
                    <% } %>
                </div>
            </div>
        </div>

        <script>
            window.addEventListener('load', ()=> {
                document.getElementById('nome_login').value = '';
                document.getElementById('nome_nome').value = '';
            });
          </script>
    </body>
</html>